apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "peevee.fullname" . }}
  labels:
    {{- include "peevee.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
